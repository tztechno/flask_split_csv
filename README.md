# flask_split_csv

```
python -m venv venv

source venv/bin/activate 

pip install -r requirements.txt

python app.py

http://127.0.0.1:5000

cd split

git init
git remote add origin 
git pull origin main https://github.com/tztechno/flask_split_csv.git
git add .
git commit -m "2024-11-30"
git push -u origin main
git push -f origin main

```